# OpenRouter Integration Requirements
# Install with: pip install -r requirements_openrouter.txt

# OpenAI library (OpenRouter uses OpenAI-compatible API)
openai>=1.0.0

# Environment variable management
python-dotenv>=1.0.0

# Note: These are IN ADDITION to your existing requirements.txt
# Make sure to also install the base project requirements